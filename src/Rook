#Rook Piece class
import Piece from Piece

class Rook(Piece):
    #args are x_coord, y_coord
    def __init__(self, args*):
        self.x = arg[0]
        self.y = arg[1]

    def __str__(self):
        return str("Rook on coords (" + self.x + "," + self.y + ")")

    def validMoves(self, board):
        pass

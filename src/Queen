#Queen Piece class
import Piece from Piece

class Queen(Piece):
    #args are x_coord, y_coord
    def __init__(self, args*):
        self.x = arg[0]
        self.y = arg[1]

    def __eq__(self, other):
        return 'Q' == other

    def __str__(self):
        return str("Queen on coords (" + self.x + "," + self.y + ")")

    def validMoves(self, board):
        pass

#King Piece class
import Piece from Piece

class King(Piece):
    #args are x_coord, y_coord
    def __init__(self, args*):
        self.x = arg[0]
        self.y = arg[1]

    def __eq__(self, other):
        return 'K' == other

    def __str__(self):
        return str("King on coords (" + self.x + "," + self.y + ")")

    def validMoves(self, board):
        pass

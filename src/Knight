#Knight Piece class
import Piece from Piece

class Knight(Piece):
    #arg[0] = x_coord
    #arg[1] = y_coord
    #arg[2] = isWhite
    def __init__(self, args*):
        self.x = arg[0]
        self.y = arg[1]
        self.isWhite = arg[2]

    def __eq__(self, other):
        return 'N' == other

    def __str__(self):
        return str("Knight on coords (" + self.x + "," + self.y + ")")

    def validMoves(self, board, new_x, new_y):
        pass
